<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".HomeActivity" >

    <Button
        android:id="@+id/DoctorsRecommendButton"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_above="@+id/RemindPageButton"
        android:layout_toRightOf="@+id/InquiryPageButton"
        android:background="@drawable/btndoctor" />

    <ImageButton
        android:id="@+id/RecordPageImageButton"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_above="@+id/InquiryPageButton"
        android:layout_toLeftOf="@+id/RemindPageButton"
        android:background="@drawable/btnnote" />

    <Button
        android:id="@+id/InquiryPageButton"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_alignBaseline="@+id/RemindPageButton"
        android:layout_alignBottom="@+id/RemindPageButton"
        android:layout_alignLeft="@+id/RecordPageImageButton"
        android:background="@drawable/btnquery" />

    <Button
        android:id="@+id/RemindPageButton"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/LogoutButton"
        android:layout_marginBottom="59dp"
        android:layout_marginRight="55dp"
        android:background="@drawable/btnts" />

    <Button
        android:id="@+id/LogoutButton"
        android:layout_width="122dp"
        android:layout_height="114dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="36dp"
        android:layout_marginTop="21dp"
        android:background="@drawable/logout" />

</RelativeLayout>
