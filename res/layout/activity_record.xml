<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background1"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".RecordActivity" >

    <ImageButton
        android:id="@+id/InsertImageButton"
        android:layout_width="125dp"
        android:layout_height="112dp"
        android:layout_alignParentTop="true"
        android:layout_alignRight="@+id/RecordCalendarView"
        android:layout_marginRight="0dp"
        android:layout_marginTop="2dp"
        android:background="@drawable/record_btn" />

    <CalendarView
        android:id="@+id/RecordCalendarView"
        android:layout_width="700dp"
        android:layout_height="450dp"
        android:layout_below="@+id/InsertImageButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="73dp" />

    <ListView
        android:id="@+id/RercordList"
        android:layout_width="600dp"
        android:layout_height="450dp"
        android:layout_below="@+id/RecordCalendarView"
        android:layout_centerHorizontal="true" >
    </ListView>

</RelativeLayout>
